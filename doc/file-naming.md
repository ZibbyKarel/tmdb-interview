# File Naming

## General Rules

- Use camel case for general file names.
- Use PascalCase for React component file names.

## Component File Rules

- Name the component file exactly the same as the exported component.
- Keep the component directory name exactly the same as the component name when a directory wrapper is used.
