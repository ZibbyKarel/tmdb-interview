# File Naming

## Rules

- Use camel case for non-component file names.
- Use PascalCase for React component file names.
- Match the component file name to the exported component name.
- When a component has a directory wrapper, match the directory name to the component name.
