# Stories

## Rules

- Create stories only when the task explicitly requests them.
- Name the default story `Playground`.
- Add an `Overview` story when the component has multiple variants worth showing together.
- Add one separate story per component variant when variant-specific stories are needed.
