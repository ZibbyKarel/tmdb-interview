# Stories

## When To Create Stories

- Create stories only when stories are explicitly requested.

## Story Structure

- Name the default story `Playground`.
- Add an `Overview` story when the component has variants to show on one page.
- Add one separate story for each component variant.

## Intention

- Keep stories predictable across the project.
- Make component exploration and review faster.
